# Requirements Audit

最終確認日: 2026-09-15

## 判定

SwiftPM ビルドと無音テスト動画の実切り取り・書き出しは成功。主要機能は実装されていますが、GUI を含む全経路を正常動作確認済みとは判定していません。今回の変更はドキュメントと再実行可能な検証コードの追加で、アプリ本体の不具合修正は含みません。

## 検証環境と結果

- macOS 26.5.2 / Apple Silicon、Swift 6.1.2（Swift 5 言語モード）。
- Xcode / XcodeGen は未導入。`xcodebuild -version` は Command Line Tools のみのため失敗。`make debug` と Xcode の `.app` ビルドは未確認。
- SwiftPM ビルド成功。実行環境のキャッシュ書き込み制限に対応するため、以下で実行しました。

```bash
CLANG_MODULE_CACHE_PATH=/tmp/mp4clipper-module-cache swift build --disable-sandbox --cache-path /tmp/mp4clipper-spm-cache --scratch-path /tmp/mp4clipper-check-build
```

`NSImage` を actor 境界越しに返す箇所で、Swift 6 言語モードではエラーになる警告があります。現行言語モードではビルド可能です。

### 実動画スモークチェック: 成功

`bash Tests/smoke-check.sh` を実行し、AVAssetWriter で作成した4秒・320×240・30fps・無音H.264 MP4を入力として、以下を確認しました。

| 検証 | 結果 |
| --- | --- |
| 動画読み込み・4秒のメタデータ取得 | 成功 |
| マーカー追加・削除 | 成功 |
| 基準2秒・前後1秒 → 1〜3秒の範囲 | 成功 |
| 基準0秒/4秒での先頭・末尾クランプ | 成功 |
| 前後0秒の範囲を無効判定 | 成功 |
| 本番 ClipExportService で1〜3秒を MP4 出力 | 成功 |
| 出力の長さ2秒（誤差0.05秒未満）・320×240 | 成功 |
| 出力動画から0.5秒位置の画像をデコード | 成功 |
| PNG/JPEG 保存・再読み込み | 成功 |
| 既存ファイルと異なる出力名の生成 | 成功 |
| 完了進捗1.0、サービス単体の不正範囲拒否 | 成功 |

制限付き実行では入力動画の生成時に `AVFoundationErrorDomain -11834 Cannot Encode` が発生しました。同じチェックをサンドボックス制限外で再実行すると成功しました。この失敗はアプリの切り取り処理へ到達する前の環境依存のものです。チェックは GUI のバッチ処理を呼ばず、モデル操作と本番サービスを直接検証します。音声や出力全フレームの時間的一致までは検証していません。

## 既知の問題（コード確認）

1. **不正範囲が失敗ジョブに残らない**: `exportClips` が無効範囲を検出すると `appendFailedJob` を呼びますが、同じ範囲検証で再び return するため履歴にもエラーにも出ません。前後とも0秒などで該当します。行内の赤字表示はあります。
2. **非同期処理中の動画切り替え**: 書き出しは開始時の asset を保持しますが、次のマーカーの範囲計算とジョブ追加は現在の `self.project` を参照します。異なる動画長を使用したり、新動画に旧動画のジョブが追加されたりする経路があります。画像生成も完了時の現在のプロジェクトへ追加します。
3. **書き出しの重複実行**: バッチ内は順次処理ですが、ボタンを再度押すと別 Task が起動します。出力名の存在確認と生成の間に排他制御はありません。
4. **再生状態の表示**: `isPlaying` は再生ボタンで反転するだけで、動画終端や読み込み時に同期しません。

現状の運用では、前後秒数を正の範囲で指定し、書き出し・画像生成が完了してから別動画を開いてください。書き出し操作も完了を待ってから行ってください。

## 未確認の範囲

- GUI 経由のファイル選択、ドラッグ&ドロップ、ショートカット、選択一括書き出しと進捗表示。SwiftPM 実行ファイルの起動は試みましたが、実行環境で Launch Services の接続エラーがあり画面操作を確認できませんでした。
- 音声保持・音ズレ、実際の長尺配信素材、MOV/M4V、HEVC、可変FPS、回転・縦動画。
- 動画末尾ちょうどのスクリーンショット、権限不足時の UI、設定の再起動後の復元。
- macOS 13 の実機互換性、署名・公証・配布。

## 実装済み

- macOS 専用 SwiftUI アプリ
- AI / クラウド / FFmpeg 非使用
- AVPlayer による動画再生
- AVAssetExportSession による動画クリップ書き出し
- AVAssetImageGenerator によるスクリーンショット抽出
- `.mp4`, `.mov`, `.m4v` のファイル選択とドラッグ&ドロップ読み込み
- ファイル名、総再生時間、解像度、FPS 表示
- 3カラム構成
- 再生 / 停止、シーク、5秒移動、1秒移動
- Space / M / S / 矢印 / Shift+矢印 / Delete のショートカット
- マーカー追加、一覧表示、ジャンプ、削除、メモ、個別オフセット調整
- マーカーから切り抜き候補範囲を計算し、0秒未満・動画長超過をクランプ
- 個別クリップ書き出し、選択マーカーの一括順次書き出し
- 書き出し進捗、成功、失敗表示
- 同名ファイルの連番回避
- 現在位置スクリーンショット候補生成
- 現在位置スクリーンショット即保存
- マーカー周辺の複数スクリーンショット候補生成
- スクリーンショット候補のサムネイル表示、大きめプレビュー、選択解除、削除、選択保存
- 出力フォルダ設定、`Clips` / `Screenshots` サブフォルダ分離
- UserDefaults による設定保存
- README / ARCHITECTURE / NEXT_TASKS

## 後回し

フレーム単位移動、プロジェクト保存、縦横比変換、品質プリセット選択は未実装です。優先順位は [NEXT_TASKS.md](NEXT_TASKS.md) を参照してください。
