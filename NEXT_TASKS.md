# Next Tasks

更新日: 2026-09-15。確認範囲は [REQUIREMENTS_AUDIT.md](REQUIREMENTS_AUDIT.md) を参照。

## 優先: 動作上の問題

- 不正範囲の書き出しが失敗ジョブにもエラーにも残らない経路を修正する。
- 書き出し・画像生成中の動画切り替えで、旧動画の処理結果が新しいプロジェクトへ混入しないようにする。バッチの範囲計算も開始時の動画長に統一する。
- 書き出しボタンの重複実行を制御する。同名回避は既存ファイルの確認のみで、出力先の予約はしていない。
- 動画終端・動画切り替え時に `isPlaying` を実際の再生状態へ同期する。

## 優先: 追加検証

- Xcode で `.app` をビルドし、ファイル選択・ドラッグ&ドロップ・ショートカット・個別/一括書き出しを GUI で確認する。
- 音声付き動画の音ズレ、長尺、MOV/M4V、HEVC、縦向き、可変フレームレートを確認する。
- 動画末尾ちょうどのスクショ取得、出力先の権限エラーを確認する。

## 改善候補

- Swift 6 言語モードに向けた `NSImage` の actor 境界の警告解消
- フレーム単位移動の追加（現状は1秒/5秒移動）
- プロジェクト保存と再読み込み
- 書き出し品質、9:16 / 1:1 などのプリセット
- 書き出し履歴と失敗ログの詳細表示、キャンセル
- マーカー/スクショ候補の一括リネーム、検索・フィルタ
- 配布向け署名・公証・アプリアイコン整備（Xcode project と `.app` ビルド手順は既存）
